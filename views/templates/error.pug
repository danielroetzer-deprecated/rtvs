extends layout

block content


    .alert.alert-danger(role='alert')
        p.
            Sorry an error occurred, please try again.
            The error has been logged for the developer.



    //Use this in development only:

    //.alert.alert-danger(role='alert')
        p.
            #{message}
            #{error.status}

    //.alert.alert-warning(role='alert')
        pre #{error.stack}
